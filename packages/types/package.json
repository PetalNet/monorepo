{
  "name": "@petalnet/types",
  "version": "0.0.0",
  "private": true,
  "description": "Type-only definitions shared across PetalNet apps. Nothing runtime — only `interface`, `type`, `enum`, `declare`.",
  "license": "UNLICENSED",
  "files": [
    "dist/",
    "src/"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "catalog:dev"
  }
}
