{
	"name": "@petalnet/effect-sveltekit",
	"version": "0.1.0",
	"private": true,
	"description": "A transparent managed Effect runtime boundary for SvelteKit requests",
	"type": "module",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"coverage": "vitest run --coverage --coverage.include=src/**/*.ts --coverage.thresholds.100",
		"test": "vitest run",
		"typecheck": "tsc -b"
	},
	"dependencies": {
		"@sveltejs/kit": "catalog:",
		"effect": "catalog:"
	},
	"devDependencies": {
		"@petalnet/tsconfig": "workspace:*",
		"@vitest/coverage-v8": "catalog:dev",
		"typescript": "catalog:dev",
		"vitest": "catalog:dev"
	}
}
