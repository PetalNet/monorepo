// @petalnet/ui — component library. Components extract here as duplicates
// emerge across apps/*; don't speculate a "shared button" before 2+ apps need it.

/** Package marker; replace with real component exports as they land. */
export const UI_PACKAGE = "@petalnet/ui";
