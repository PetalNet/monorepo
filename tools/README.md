# tools/

Repo-internal scripts. TypeScript only (`.mts`) — no shell scripts, for Windows
parity. Not shipped with any app, not consumed by Vite+, not workspaced.

Empty for now; add `.mts` helpers here as repeatable repo chores emerge.
