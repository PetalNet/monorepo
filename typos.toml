[files]
extend-exclude = [
	"pnpm-lock.yaml",
	"**/dist/**",
	"**/build/**",
	"**/.svelte-kit/**",
]

[default.extend-words]
# Proper nouns + identifiers that look like typos but aren't.
Hass = "Hass"
hass = "hass"
