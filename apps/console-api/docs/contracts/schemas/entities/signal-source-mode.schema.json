{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://petalcat.dev/schemas/console/entities/signal-source-mode.schema.json",
	"title": "Signal source delivery mode (GET /signal-sources)",
	"description": "Fleet-scoped delivery override for one exact emission source.service. Development mode suppresses off-console alert delivery while a feed is being built; normal mode restores the ordinary subscription and safety policy. The same audited signal.source_mode op is available to humans and agents with fleet operator access.",
	"type": "object",
	"required": ["source_service", "mode", "updated_at", "updated_by"],
	"additionalProperties": true,
	"properties": {
		"source_service": { "type": "string", "maxLength": 64 },
		"mode": { "enum": ["normal", "development"] },
		"note": { "type": ["string", "null"], "maxLength": 240 },
		"updated_at": { "type": "string", "format": "date-time" },
		"updated_by": { "type": "string" }
	}
}
