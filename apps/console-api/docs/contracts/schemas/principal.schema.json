{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://petalcat.dev/schemas/console/principal.schema.json",
  "title": "Principal (server-stamped caller identity)",
  "description": "Resolved by console-api after authentication (Authentik forwardAuth for humans, bearer tokens for agents/services). NEVER trusted from the client. sender_class for dispatcher interop derives from kind + tier.",
  "type": "object",
  "required": ["schema_version", "kind", "id", "tiers", "lanes", "scopes"],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "const": 1 },
    "kind": { "enum": ["human", "agent", "system"] },
    "id": { "type": "string", "description": "Username (human), agent:<handle>, or system:<service>." },
    "tiers": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Permission levels (DB-defined rows, not an enum): owner, moderator, collaborator, guest, + future tiers. From Authentik groups / token scope."
    },
    "lanes": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Capability lanes for the command plane (operator, admin, term_admin, readonly, ...)."
    },
    "scopes": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Resolved readable scope tags (zookie-fenced snapshot; revocation invalidates)."
    },
    "zookie": { "type": "string", "description": "Consistency token for the grant snapshot this principal was resolved at." }
  }
}
