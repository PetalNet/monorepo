services:
  bot:
    build: .
    container_name: courier
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
    environment:
      - MATRIX_CONFIG=/app/config.yaml
      - RUST_LOG=info,courier_ai=debug
