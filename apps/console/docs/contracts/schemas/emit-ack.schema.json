{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://petalcat.dev/schemas/console/emit-ack.schema.json",
	"title": "Emit acknowledgement (202 body for POST /emit and per-item results of /emit/batch)",
	"type": "object",
	"required": ["seq"],
	"additionalProperties": true,
	"properties": {
		"seq": {
			"type": "integer",
			"description": "Lake seq of the durable row. A duplicate emission id returns the ORIGINAL seq (idempotent accept)."
		},
		"duplicate": { "type": "boolean", "default": false },
		"error": {
			"type": ["object", "null"],
			"required": ["code", "message", "retryable"],
			"properties": {
				"code": { "type": "string" },
				"message": { "type": "string" },
				"retryable": { "type": "boolean" }
			},
			"description": "Batch items only: per-item rejection (source_mismatch, scope_denied, namespace_reserved, rate_limited, payload_too_large, secret_detected)."
		}
	}
}
