# Built shell — board review (after) + resolutions

Consolidated 5-hat board on the BUILT surface (screenshots inspected). Verdict:
**ship-after-fixes**. Fidelity: ~17/20 shell decisions realized. All findings resolved:

| #   | Sev | Finding                                                                                                                                                                                      | Resolution                                                                                                                                                                                                                                                                                            |
| --- | --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | P1  | `term.watch`/`signal.snooze` fix_op args don't match ops.json (`term.watch` needs host/tmux_session/pane_id, not handle; `signal.snooze` needs type_pattern/duration_s) → dead crack buttons | `mock.ts` binds exact ops.json args (term.watch `{host,tmux_session,pane_id}`); dropped `signal.snooze` from a live crash (wrong instinct to mute a crashed agent's signal).                                                                                                                          |
| 2   | P1  | `attention.snooze` sent `duration:"1h"`; schema wants `duration_s` int                                                                                                                       | `AttentionCard` sends `{id, duration_s:3600}`.                                                                                                                                                                                                                                                        |
| 3   | P2  | Crack card = 6 buttons (two near-identical mute verbs) → triage overload                                                                                                                     | Lead with Restart (primary) + Watch session; Ack/Snooze/Done collapse into an overflow (`ellipsis`); signal-mute removed.                                                                                                                                                                             |
| 4   | P2  | Docked ask bar has a resting inset ring                                                                                                                                                      | **Kept** — foundations §3.1 explicitly permits inset hairline rings for the snackbar AND the dock; `--shadow-pop` would violate §3.4 (dialog-only shadow). Board false positive; documented in the component. Added the outset petal focus ring on top.                                               |
| 5   | P2  | Live mode painted mock fixtures under "Can't verify" (fake state, veto #20)                                                                                                                  | `liveEmptyCockpit()` + `connected` flag → honest "Can't verify the neighborhood" placeholder, no fabricated rail/board/mail. **Also fixed a live-mode 500**: `readMe` failure now degrades to an offline principal (§1 blast radius: honestly dark, never a crash) with the assistant-down ask state. |
| 6   | P2  | Serif spent on greeting AND "Town Hall" AND crack subline (§7 budget: one)                                                                                                                   | Town Hall subtitle → sans italic; crack subline → sans. Serif = the greeting row only.                                                                                                                                                                                                                |
| 7   | P2  | "open the house" linked the agent handle, not its host                                                                                                                                       | `blast_radius.host` added + consumed; links `/hosts?host=.14`.                                                                                                                                                                                                                                        |
| 8   | P3  | Off-grid values (3px gaps, 1px radii) in HouseTile/inline-stat                                                                                                                               | Snapped to `--s-1` (4px) + `--r-xs` (2px).                                                                                                                                                                                                                                                            |
| 9   | P3  | Icon-rail hides numeric badges with no fallback                                                                                                                                              | `.rail-dot` fallback dot shown in collapsed mode when count > 0.                                                                                                                                                                                                                                      |
| 10  | P3  | `needs_you` verdict rendered a green motif over a needs-you state                                                                                                                            | Neutral tone + `circle-help` icon (not green `circle-check`) on hero + sidebar.                                                                                                                                                                                                                       |
| 11  | P3  | Right-click-ask wired only on generated panels; hint promises "anything"                                                                                                                     | Delegated `oncontextmenu` over the cockpit; `[data-ask]` on Town Hall + houses makes them interrogable.                                                                                                                                                                                               |
| 12  | P3  | House roof band painted decorative jade (charter reserves jade)                                                                                                                              | Roof band → `--rule-strong` (neutral); jade kept for proof + Janet.                                                                                                                                                                                                                                   |
