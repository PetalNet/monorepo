# Shell plan — board review (before) + resolutions

Consolidated 5-hat board (eli-design, ops-operator, wayfinder-fidelity, lore-warden,
backend-realist). Verdict: **proceed-with-changes**. Fidelity: ~16/19 shell decisions covered.
All findings resolved in the plan/code before building components:

| #   | Sev | Finding                                                                                             | Resolution                                                                                                                                                                                                 |
| --- | --- | --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | P1  | Client re-derives incident collapse key → mis-groups vs server                                      | `derive.incidentKey` uses server `incident_key` only; null → own incident (`solo:<id>`). Never computes collapse/blast-radius/op-args from subject.                                                        |
| 2   | P1  | ActionRow bound to spec-name aliases (`governance.pause`), which are docs-only                      | `api/ops.ts` binds EXACT `ops.json` wire names (agent.restart, governance.action, signal.snooze, term.watch, attention.\*, dashboard.set_home). Aliases display-only.                                      |
| 3   | P1  | No lane gating on nav/ActionRow visibility                                                          | `nav.visibleNav(lanes)` hides Terminal unless term_admin; `ops.canSeeOp(op, lanes)` gates every OpButton. (Client gate is cosmetic; server enforces authz∩lane.)                                           |
| 4   | P1  | "Fine" check must be three-part positive evidence + explicit "Can't verify"                         | `derive.healthVerdict` returns fine/cracked/cant_verify/needs_you from busFresh AND fleetFresh AND no-active-P0. Hero + sidebar state line render the three-way truth incl. "Can't verify. Bus silent Nm." |
| 5   | P2  | Radius contradiction: mock uses 12px on cockpit cards; §3.4 reserves 12px for assistant Panels only | Shell chrome cards (TownHall/FleetRail/MailRail) = `--r-xs` (2px). `--r-md` (12px) reserved strictly for assistant-composed `Panel`. Mock has the bug; spec §3.4 wins.                                     |
| 6   | P2  | grade vs severity conflation                                                                        | Two named derivations kept: attention order/cockpit badge on `grade`; SignalRow/severity→label on emission `severity`.                                                                                     |
| 7   | P2  | Missing ProgressStream/BoardCard/Countdown/BudgetLight/Drawer                                       | Deferred with owner to their surface PRs (Work: BoardCard/Countdown; Agents: ProgressStream/BudgetLight/Drawer). Shell ships generic AttentionCard (renders any grade + fix_ops). Logged in BLOCKERS.md.   |
| 8   | P2  | `/` cockpit live-mode reads are 2nd-pass                                                            | Cockpit stays mock-default; live-mode gated on 2nd-pass endpoints. Logged in BLOCKERS.md.                                                                                                                  |
| 9   | P2  | Per-source freshness windows, not one 90s                                                           | `derive.WINDOW` already per-source; fleet 90 / heartbeat 30 / registry 90-300 / matrix 120 / bus 90.                                                                                                       |
| 10  | P3  | Sign-face density: counts in serif                                                                  | Signage NOUNS (Town Hall / The Neighborhood) in serif sign-small; counts + meta in Geist mono. One 32px sign moment per screen (the greeting).                                                             |
| 11  | P3  | Me/roster bypass ReadEnvelope                                                                       | `readMe` returns bare `Me`; roster will be a bare join, not enveloped.                                                                                                                                     |
