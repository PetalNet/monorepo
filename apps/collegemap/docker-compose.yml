services:
  map:
    build:
      # The Dockerfile installs from the workspace lockfile, so the build
      # context has to be the monorepo root, not this directory.
      context: ../..
      dockerfile: apps/collegemap/Dockerfile
    ports:
      - "4718:3000"
    volumes:
      - map-data:/data
    environment:
      - DATABASE_URL=file:/data/local.db

volumes:
  map-data:
