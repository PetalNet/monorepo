# Local environment
.env

# Per-tool runtime configs
config.yaml

# Bot state
bot-store/
session.json
config.yaml
data/

# Build artifacts (if running from crate dir)
/target/

# Node.js (never commit these)
node_modules/
dist/
package.json
package-lock.json

.DS_Store

# Real deployment configs + backups (secret-bearing — never commit)
config.docker.yaml
config.*.yaml
!config.example.yaml
*.bak-*
