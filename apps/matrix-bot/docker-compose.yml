services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.docker.yaml:/app/config.yaml:ro
      - ./data:/app/data
    environment:
      - RUST_LOG=info,plugin_ai=debug
